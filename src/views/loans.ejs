<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bibly - <%= user && user.role === 'admin' ? 'Gestão de Empréstimos' : 'Meus Empréstimos' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <%- include('partials/navbar') %>

    <main class="flex-grow py-10">
        <div class="max-w-[95%] mx-auto px-4">
            <% if (typeof user !== 'undefined' && user && user.name !== 'Visitante') { %>
                <header class="text-center mb-10">
                    <h1 class="text-4xl font-extrabold text-gray-900 mb-2">
                        <%= user.role === 'admin' ? 'Empréstimos Realizados' : 'Meus Empréstimos' %>
                    </h1>
                    <p class="text-lg text-gray-600">
                        <%= user.role === 'admin' ? 'Veja os últimos empréstimos feitos no sistema e o histórico completo' : 'Veja quais exemplares você já pegou emprestado.' %>
                    </p>
                </header>

                <% if (user.role === 'admin') { %>
                    <div class="mb-8 max-w-2xl mx-auto">
                        <div class="relative group">
                            <i class="fas fa-search absolute left-5 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-indigo-500 transition-colors"></i>
                            <input type="text" id="userSearch" placeholder="Pesquisar por nome do usuário..." 
                                class="w-full pl-12 pr-6 py-4 bg-white rounded-2xl shadow-sm border border-gray-100 focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-500 outline-none transition-all font-medium text-gray-700">
                        </div>
                    </div>
                <% } %>

                <div class="bg-white rounded-3xl shadow-xl overflow-hidden border border-gray-100">
                    <div class="overflow-x-auto">
                        <table class="w-full text-left border-collapse whitespace-nowrap" id="loansTable">
                            <thead>
                                <tr class="bg-gray-50 border-b border-gray-100">
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">ID</th>
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Usuário</th>
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Livro</th>
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Empréstimo</th>
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Devolução</th>
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Status</th>
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Valor</th>
                                    <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Multa</th>
                                    <% if (user.role === 'admin') { %>
                                        <th class="px-6 py-4 text-xs font-black uppercase tracking-widest text-gray-400">Ações</th>
                                    <% } %>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-50">
                                <% if (loans.length > 0) { %>
                                    <% loans.forEach(loan => { %>
                                        <tr class="hover:bg-indigo-50/30 transition-all loan-row">
                                            <td class="px-6 py-4 font-bold text-gray-400">#<%= loan.id %></td>
                                            <td class="px-6 py-4">
                                                <a href="/usuarios/<%= loan.user_id %>" class="user-name-cell font-bold text-gray-900 hover:text-indigo-600 hover:underline transition-colors">
                                                    <%= loan.user_name %>
                                                </a>
                                            </td>
                                            <td class="px-6 py-4 font-medium text-indigo-600"><%= loan.book_title %></td>
                                            <td class="px-6 py-4 text-gray-500 font-semibold"><%= loan.loan_date %></td>
                                            <td class="px-6 py-4 text-indigo-400 font-semibold"><%= loan.due_date %></td>
                                            <td class="px-6 py-4">
                                                <span class="px-3 py-1 <%= loan.status === 'ativo' || loan.status === 'em aberto' ? 'bg-green-100 text-green-700' : (loan.status === 'vencido' ? 'bg-red-100 text-red-700' : 'bg-gray-100 text-gray-400') %> rounded-full text-xs font-black uppercase tracking-widest">
                                                    <%= loan.status %>
                                                </span>
                                            </td>
                                            <td class="px-6 py-4 font-bold text-gray-900">R$ <%= loan.total_price.toFixed(2).replace('.', ',') %></td>
                                            <td class="px-6 py-4 font-bold <%= loan.status === 'vencido' ? 'text-red-600' : 'text-gray-900' %>">
                                                R$ <%= loan.fine %>
                                            </td>
                                            <% if (user.role === 'admin') { %>
                                                <td class="px-6 py-4">
                                                    <% if (loan.status !== 'fechado') { %>
                                                        <form action="/emprestimos/<%= loan.id %>/fechar" method="POST">
                                                            <button type="submit" class="text-xs font-black bg-indigo-600 text-white px-3 py-1 rounded-lg hover:bg-indigo-700 transition-all">
                                                                FECHAR
                                                            </button>
                                                        </form>
                                                    <% } else { %>
                                                        <span class="text-xs font-bold text-gray-300">CONCLUÍDO</span>
                                                    <% } %>
                                                </td>
                                            <% } %>
                                        </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="<%= user.role === 'admin' ? 9 : 8 %>" class="px-6 py-20 text-center">
                                            <i class="fas fa-handshake-slash text-gray-200 text-6xl mb-4 block"></i>
                                            <p class="text-gray-500 font-bold italic">
                                                <%= user.role === 'admin' ? 'Nenhum registro encontrado no sistema.' : 'Você ainda não realizou nenhum empréstimo.' %>
                                            </p>
                                        </td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            <% } else { %>
                <div class="max-w-md mx-auto mt-20 text-center bg-white p-10 rounded-3xl shadow-xl border border-gray-100">
                    <div class="w-20 h-20 bg-indigo-100 text-indigo-600 rounded-full flex items-center justify-center mx-auto mb-6 text-3xl shadow-inner">
                        <i class="fas fa-lock"></i>
                    </div>
                    <h2 class="text-2xl font-black text-gray-900 mb-4 italic">Acesso Restrito</h2>
                    <p class="text-gray-500 mb-8 font-medium italic">Não é possível visualizar o histórico sem estar logado.</p>
                    <a href="/usuarios/novo" class="block w-full py-4 bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-black rounded-2xl shadow-lg hover:shadow-indigo-200 hover:-translate-y-1 transition-all active:scale-95">
                        CRIAR MINHA CONTA
                    </a>
                </div>
            <% } %>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script>
        const searchInput = document.getElementById('userSearch');
        if (searchInput) {
            searchInput.addEventListener('input', function() {
                const term = this.value.toLowerCase();
                const rows = document.querySelectorAll('.loan-row');
                
                rows.forEach(row => {
                    const userNameCell = row.querySelector('.user-name-cell');
                    if (userNameCell) {
                        const userName = userNameCell.textContent.toLowerCase();
                        row.style.display = userName.includes(term) ? '' : 'none';
                    }
                });
            });
        }
    </script>
</body>
</html>