<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bibly - Devoluções</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <%- include('partials/navbar') %>

    <main class="flex-grow py-10">
        <div class="max-w-7xl mx-auto px-4">
            <header class="flex flex-col items-center text-center mb-10 gap-6">
                <div>
                    <h1 class="text-4xl font-extrabold text-gray-900 mb-2">Histórico de Devoluções</h1>
                    <p class="text-lg text-gray-600">Controle veja os registros das suas devoluções de livros.</p>
                </div>
            </header>

            <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full text-left">
                        <thead>
                            <tr class="bg-gray-50 border-b border-gray-100 text-gray-400 text-xs font-black uppercase tracking-widest">
                                <th class="px-6 py-5">Livro</th>
                                <th class="px-6 py-5">Leitor</th>
                                <th class="px-6 py-5">Empréstimo</th>
                                <th class="px-6 py-5">Devolução</th>
                                <th class="px-6 py-5">Multa</th>
                                <th class="px-6 py-5 text-right">Status</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-50">
                            <% if (typeof returns !== 'undefined' && returns.length > 0) { %>
                                <% returns.forEach(returnItem => { %>
                                    <tr class="hover:bg-gray-50/50 transition-colors group">
                                        <td class="px-6 py-4">
                                            <div class="font-bold text-gray-900 group-hover:text-indigo-600 transition-colors"><%= returnItem.bookTitle %></div>
                                        </td>
                                        <td class="px-6 py-4 text-gray-600 font-medium"><%= returnItem.userName %></td>
                                        <td class="px-6 py-4 text-gray-500 text-sm italic"><%= returnItem.loanDate %></td>
                                        <td class="px-6 py-4 text-gray-900 font-semibold"><%= returnItem.returnDate %></td>
                                        <td class="px-6 py-4 font-black <%= returnItem.penalty > 0 ? 'text-red-500' : 'text-green-500' %>">
                                            <%= returnItem.penalty > 0 ? 'R$ ' + returnItem.penalty : '-' %>
                                        </td>
                                        <td class="px-6 py-4 text-right">
                                            <% 
                                                let badgeClass = returnItem.status === 'Dentro do prazo' ? 'bg-green-100 text-green-600' : 'bg-orange-100 text-orange-600';
                                            %>
                                            <span class="px-3 py-1 rounded-full text-xs font-bold <%= badgeClass %>">
                                                <%= returnItem.statusDisplay %>
                                            </span>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="6" class="px-6 py-20 text-center">
                                        <div class="flex flex-col items-center">
                                            <i class="fas fa-history text-gray-200 text-5xl mb-4"></i>
                                            <p class="text-gray-400 italic font-medium text-lg">Nenhum registo de devolução encontrado.</p>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-gray-900 text-gray-400 py-8 mt-auto">
        <div class="max-w-7xl mx-auto px-4 text-center">
            <p>&copy; 2026 Bibly. Todos os direitos reservados.</p>
        </div>
    </footer>

    <script src="/js/main.js"></script>
    <script src="/js/returns.js"></script>
</body>
</html>