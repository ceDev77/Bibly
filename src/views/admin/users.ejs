<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bibly - Gerenciar Usuários</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <%- include('../partials/navbar') %>

    <main class="flex-grow py-10">
        <div class="max-w-7xl mx-auto px-4">
            <header class="text-center mb-10">
                <h1 class="text-4xl font-extrabold text-gray-900 mb-2 italic">Gerenciar Usuários</h1>
                <p class="text-lg text-gray-600 font-medium italic">Controle de acessos e permissões dos membros da biblioteca.</p>
            </header>

            <div class="mb-8 max-w-2xl mx-auto">
                <div class="relative group">
                    <i class="fas fa-search absolute left-5 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-indigo-500 transition-colors"></i>
                    <input type="text" id="userSearch" placeholder="Pesquisar por nome do usuário..." 
                        class="w-full pl-12 pr-6 py-4 bg-white rounded-2xl shadow-sm border border-gray-100 focus:ring-4 focus:ring-indigo-500/10 focus:border-indigo-500 outline-none transition-all font-medium text-gray-700">
                </div>
            </div>

            <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full text-left border-collapse">
                        <thead class="bg-gray-50 border-b border-gray-100">
                            <tr>
                                <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase tracking-wider">Usuário</th>
                                <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase tracking-wider">E-mail</th>
                                <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase tracking-wider">CPF</th>
                                <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase tracking-wider">Telefone</th>
                                <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase tracking-wider text-center">Ações</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-100">
                            <% if (typeof users !== 'undefined' && users.length > 0) { %>
                                <% users.filter(u => u.role !== 'admin').forEach(u => { %>
                                    <tr class="hover:bg-gray-50 transition-colors">
                                        <td class="px-6 py-4">
                                            <div class="flex items-center gap-3">
                                                <div class="w-10 h-10 rounded-full bg-indigo-100 text-indigo-600 flex items-center justify-center font-bold">
                                                    <%= (u.name && u.name.length > 0) ? u.name.charAt(0).toUpperCase() : '?' %>
                                                </div>
                                                <span class="font-bold text-gray-900"><%= u.name %></span>
                                            </div>
                                        </td>
                                        <td class="px-6 py-4 text-gray-600 italic">
                                            <%= u.email %>
                                        </td>
                                        <td class="px-6 py-4 text-gray-600 font-medium">
                                            <%= u.cpf || 'Não informado' %>
                                        </td>
                                        <td class="px-6 py-4 text-gray-600 font-medium">
                                            <%= u.phone || 'Não informado' %>
                                        </td>
                                        <td class="px-6 py-4 text-center">
                                            <div class="flex items-center justify-center gap-2">
                                                <button onclick="viewUser('<%= u.id %>')" class="w-9 h-9 flex items-center justify-center bg-gray-100 text-gray-600 rounded-lg hover:bg-indigo-600 hover:text-white transition-all shadow-sm" title="Informações">
                                                    <i class="fas fa-info-circle text-sm"></i>
                                                </button>
                                                <button onclick="deleteUser('<%= u.id %>')" class="w-9 h-9 flex items-center justify-center bg-gray-100 text-red-500 rounded-lg hover:bg-red-500 hover:text-white transition-all shadow-sm" title="Excluir">
                                                    <i class="fas fa-trash-alt text-sm"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="5" class="px-6 py-20 text-center">
                                        <div class="flex flex-col items-center">
                                            <i class="fas fa-users text-gray-200 text-5xl mb-4"></i>
                                            <p class="text-gray-400 italic font-medium text-lg">Nenhum usuário cadastrado.</p>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script>
        function viewUser(id) {
            window.location.href = '/usuarios/' + id;
        }

        function deleteUser(id) {
            Swal.fire({
                title: 'Tem certeza?',
                text: "Esta ação não pode ser desfeita!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#4F46E5',
                cancelButtonColor: '#EF4444',
                confirmButtonText: 'Sim, excluir!',
                cancelButtonText: 'Cancelar',
                reverseButtons: true,
                borderRadius: '1.5rem',
                customClass: {
                    popup: 'rounded-3xl shadow-2xl border border-gray-100 italic'
                }
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch('/usuarios/' + id, { method: 'DELETE' })
                    .then(res => {
                        if (res.ok) {
                            Swal.fire({
                                title: 'Excluído!',
                                text: 'Usuário removido com sucesso.',
                                icon: 'success',
                                timer: 2000,
                                showConfirmButton: false
                            }).then(() => {
                                window.location.reload();
                            });
                        } else {
                            Swal.fire('Erro!', 'Não foi possível excluir o usuário.', 'error');
                        }
                    });
                }
            });
        }

        const searchInput = document.getElementById('userSearch');
        if (searchInput) {
            searchInput.addEventListener('input', function() {
                const term = this.value.toLowerCase();
                const rows = document.querySelectorAll('tbody tr');
                
                rows.forEach(row => {
                    const userNameCell = row.querySelector('.font-bold.text-gray-900');
                    if (userNameCell) {
                        const userName = userNameCell.textContent.toLowerCase();
                        row.style.display = userName.includes(term) ? '' : 'none';
                    }
                });
            });
        }
    </script>
</body>
</html>