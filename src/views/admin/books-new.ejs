<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bibly - Cadastro de Livro</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <%- include('../partials/navbar') %>

    <main class="flex-grow py-12 px-4">
        <div class="max-w-2xl mx-auto">
            <header class="text-center mb-10">
                <h1 class="text-4xl font-extrabold text-gray-900 mb-2">Cadastro de Livro</h1>
                <p class="text-lg text-gray-600 italic">Preencha os dados para cadastrar um novo livro no acervo.</p>
            </header>

            <div class="bg-white p-8 rounded-3xl shadow-xl border border-gray-100">
                <form id="bookForm" action="/livros" method="POST" class="space-y-6">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="space-y-2 md:col-span-2">
                            <label for="title" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-book text-indigo-500 mr-2"></i> Título
                            </label>
                            <input type="text" id="title" name="title" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" placeholder="Ex: Dom Casmurro" required>
                        </div>

                        <div class="space-y-2">
                            <label for="author" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-user text-indigo-500 mr-2"></i> Autor
                            </label>
                            <input type="text" id="author" name="author" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" placeholder="Ex: Machado de Assis" required>
                        </div>

                        <div class="space-y-2">
                            <label for="year" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-calendar text-indigo-500 mr-2"></i> Ano de Publicação
                            </label>
                            <input type="number" id="year" name="year" min="0" step="1" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" placeholder="Ex: 1899">
                        </div>

                        <div class="space-y-2">
                            <label for="category" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-tag text-indigo-500 mr-2"></i> Categoria
                            </label>
                            <input type="text" id="category" name="category" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" placeholder="Ex: Romance">
                        </div>

                        <div class="space-y-2">
                            <label for="stock" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-layer-group text-indigo-500 mr-2"></i> Estoque
                            </label>
                            <input type="number" id="stock" name="stock" min="1" step="1" value="1" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" required>
                        </div>

                        <div class="space-y-2 md:col-span-2">
                            <label for="isbn" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-barcode text-indigo-500 mr-2"></i> ISBN
                            </label>
                            <input type="text" id="isbn" name="isbn" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" placeholder="Ex: 978-85...">
                        </div>

                        <div class="space-y-2 md:col-span-2">
                            <label for="image_url" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-image text-indigo-500 mr-2"></i> URL da Imagem de Capa
                            </label>
                            <input type="url" id="image_url" name="image_url" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" placeholder="https://exemplo.com/imagem.jpg">
                        </div>

                        <div class="space-y-2 md:col-span-2">
                            <label for="description" class="block text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-align-left text-indigo-500 mr-2"></i> Sinopse e Detalhes
                            </label>
                            <textarea id="description" name="description" rows="4" class="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition-all" placeholder="Breve resumo sobre a obra..."></textarea>
                        </div>
                    </div>

                    <div class="pt-6 border-t border-gray-100 flex flex-col sm:flex-row gap-4">
                        <button type="submit" class="flex-1 py-4 bg-gradient-to-r from-indigo-500 to-purple-600 text-white font-bold rounded-xl shadow-lg hover:from-indigo-600 hover:to-purple-700 transition-all transform hover:-translate-y-1">
                            <i class="fas fa-plus mr-2"></i> Cadastrar Livro
                        </button>
                        <a href="/livros" class="px-8 py-4 bg-gray-100 text-gray-600 font-bold rounded-xl hover:bg-gray-200 transition-all text-center">
                            Cancelar
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>