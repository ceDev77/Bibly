<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <%- include('../partials/navbar') %>

    <main class="flex-grow py-10">
        <div class="max-w-7xl mx-auto px-4">
            <header class="text-center mb-10">
                <h1 class="text-4xl font-extrabold text-gray-900 mb-2 italic">Balanço Financeiro</h1>
                <p class="text-lg text-gray-600 font-medium italic">Histórico de arrecadação de empréstimos e multas.</p>
            </header>

            <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 mb-8">
                <form action="/admin/balanco" method="GET" class="flex flex-wrap items-end gap-4">
                    <div class="flex-1 min-w-[200px]">
                        <label class="block text-sm font-bold text-gray-700 mb-2 italic">Data Inicial</label>
                        <input type="date" name="startDate" value="<%= filters.startDate %>" 
                            class="w-full px-4 py-2 rounded-xl border border-gray-200 focus:ring-2 focus:ring-indigo-500 outline-none">
                    </div>
                    <div class="flex-1 min-w-[200px]">
                        <label class="block text-sm font-bold text-gray-700 mb-2 italic">Data Final</label>
                        <input type="date" name="endDate" value="<%= filters.endDate %>" 
                            class="w-full px-4 py-2 rounded-xl border border-gray-200 focus:ring-2 focus:ring-indigo-500 outline-none">
                    </div>
                    <button type="submit" class="px-8 py-2 bg-indigo-600 text-white font-bold rounded-xl hover:bg-indigo-700 transition-all shadow-md">
                        <i class="fas fa-filter mr-2"></i>Filtrar
                    </button>
                    <a href="/admin/balanco" class="px-4 py-2 text-gray-500 font-medium hover:text-gray-700 italic">Limpar</a>
                </form>
            </div>

            <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                <table class="w-full text-left border-collapse">
                    <thead class="bg-gray-50 border-b border-gray-100">
                        <tr>
                            <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase italic">ID Emp.</th>
                            <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase italic">Usuário</th>
                            <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase italic">Data Fechamento</th>
                            <th class="px-6 py-4 text-xs font-bold text-gray-500 uppercase italic">Valor Total</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        <% if (records.length > 0) { %>
                            <% records.forEach(record => { %>
                                <tr class="hover:bg-gray-50 transition-colors">
                                    <td class="px-6 py-4 text-gray-600 font-bold">#<%= record.loan_id %></td>
                                    <td class="px-6 py-4 text-gray-900 font-medium"><%= record.user_name %></td>
                                    <td class="px-6 py-4 text-gray-600">
                                        <%= new Date(record.received_at).toLocaleDateString('pt-BR') %>
                                    </td>
                                    <td class="px-6 py-4 text-green-600 font-black">
                                        R$ <%= record.amount.toFixed(2) %>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="px-6 py-10 text-center text-gray-400 italic">Nenhum registro encontrado para este período.</td>
                            </tr>
                        <% } %>
                    </tbody>
                    <tfoot class="bg-indigo-50 border-t-2 border-indigo-100">
                        <tr>
                            <td colspan="3" class="px-6 py-4 text-right font-black text-indigo-900 uppercase italic">Total Acumulado:</td>
                            <td class="px-6 py-4 text-xl font-black text-indigo-600">R$ <%= totalBalance %></td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>