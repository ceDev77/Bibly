<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <%- include('../partials/navbar') %>

    <main class="flex-grow py-10">
        <div class="max-w-4xl mx-auto px-4">
            <a href="/usuarios" class="text-indigo-600 font-bold hover:underline flex items-center gap-2 mb-8">
                <i class="fas fa-arrow-left text-xs"></i> Voltar para lista
            </a>

            <div class="bg-white rounded-3xl shadow-xl overflow-hidden border border-gray-100">
                <div class="p-8 md:p-12">
                    <div class="flex flex-col md:flex-row items-center gap-8 mb-10 pb-10 border-b border-gray-100">
                        <div class="w-32 h-32 bg-indigo-100 rounded-full flex items-center justify-center text-indigo-500 text-5xl shadow-inner">
                            <i class="fas fa-user"></i>
                        </div>
                        <div class="text-center md:text-left">
                            <h1 class="text-4xl font-black text-gray-900 mb-2 italic"><%= targetUser.name %></h1>
                            <div class="flex flex-wrap justify-center md:justify-start gap-3">
                                <span class="px-4 py-1 bg-indigo-600 text-white rounded-full text-xs font-black uppercase tracking-widest">
                                    <%= targetUser.role === 'admin' ? 'ADMINISTRADOR' : 'LEITOR' %>
                                </span>
                                <span class="px-4 py-1 bg-gray-100 text-gray-500 rounded-full text-xs font-bold uppercase tracking-widest border border-gray-200">
                                    ID: #<%= targetUser.id %>
                                </span>
                            </div>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
                        <div class="space-y-6">
                            <div class="flex items-center gap-4">
                                <div class="w-12 h-12 bg-gray-50 rounded-2xl flex items-center justify-center text-indigo-500 border border-gray-100">
                                    <i class="fas fa-envelope"></i>
                                </div>
                                <div>
                                    <span class="block text-xs font-black text-gray-400 uppercase tracking-widest">Endereço de E-mail</span>
                                    <p class="text-lg font-bold text-gray-800"><%= targetUser.email %></p>
                                </div>
                            </div>
                            <div class="flex items-center gap-4">
                                <div class="w-12 h-12 bg-gray-50 rounded-2xl flex items-center justify-center text-indigo-500 border border-gray-100">
                                    <i class="fas fa-phone"></i>
                                </div>
                                <div>
                                    <span class="block text-xs font-black text-gray-400 uppercase tracking-widest">Telefone</span>
                                    <p class="text-lg font-bold text-gray-800"><%= targetUser.phone || 'Não informado' %></p>
                                </div>
                            </div>
                            <div class="flex items-center gap-4">
                                <div class="w-12 h-12 bg-gray-50 rounded-2xl flex items-center justify-center text-indigo-500 border border-gray-100">
                                    <i class="fas fa-id-card"></i>
                                </div>
                                <div>
                                    <span class="block text-xs font-black text-gray-400 uppercase tracking-widest">CPF</span>
                                    <p class="text-lg font-bold text-gray-800"><%= targetUser.cpf || 'Não informado' %></p>
                                </div>
                            </div>
                        </div>
                        <div class="space-y-6">
                            <div class="flex items-center gap-4">
                                <div class="w-12 h-12 bg-gray-50 rounded-2xl flex items-center justify-center text-indigo-500 border border-gray-100">
                                    <i class="fas fa-birthday-cake"></i>
                                </div>
                                <div>
                                    <span class="block text-xs font-black text-gray-400 uppercase tracking-widest">Data de Nascimento</span>
                                    <p class="text-lg font-bold text-gray-800"><%= targetUser.birthDate || 'Não informado' %></p>
                                </div>
                            </div>
                            <div class="flex items-center gap-4">
                                <div class="w-12 h-12 bg-gray-50 rounded-2xl flex items-center justify-center text-indigo-500 border border-gray-100">
                                    <i class="fas fa-map-marker-alt"></i>
                                </div>
                                <div>
                                    <span class="block text-xs font-black text-gray-400 uppercase tracking-widest">Endereço</span>
                                    <p class="text-lg font-bold text-gray-800"><%= targetUser.address || 'Não informado' %></p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>